#!/bin/sh
# Xoa "Co-authored-by: Cursor" khi commit tu Cursor/VSCode
FILE="$1"
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
if [ -n "$FILE" ] && [ -f "$FILE" ] && [ -f "$ROOT/scripts/strip-cursor-coauthor.js" ]; then
  node "$ROOT/scripts/strip-cursor-coauthor.js" "$FILE" 2>/dev/null || true
fi
exit 0
